# SSH target for deploy.sh — user@host, or a Host alias from ~/.ssh/config
HOMELAB_HOST=homelab@192.168.1.10

# Absolute path on the remote where the app should live
HOMELAB_PATH=/home/homelab/immich-cast
